#include <sqlite3.h>

compile program with the sqlite3 source file "/usr/include/sqlite3/sqlite3.c".
that is copy "sqlite3.c" into source code tree,or use "#include" preprocessor
to include "sqlite3.c"(but deprecated).